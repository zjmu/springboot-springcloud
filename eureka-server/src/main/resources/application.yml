server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server
  client:
    register-with-eureka: false #自身作为服务器不作为客户端
    fetch-registry: false #不注册自身
    service-url:
      defaultZone: http://localhost:8761/eureka/ #服务IP地址